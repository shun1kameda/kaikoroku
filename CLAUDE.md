# CLAUDE.md — コタオデッセイ（Kota Odyssey）

このファイルは、このリポジトリで作業する Claude Code 向けの案内です。まず読んでください。
詳しい企画は `game_design_document_template.md`、経緯は `HANDOFF.md` を参照。

## プロジェクト概要
- Godot 4（GDScript）で作る3Dアクションゲーム。将来 HTML5 書き出し想定。
- スーパーマリオオデッセイの「操作の気持ちよさ」と「箱庭レベルデザインの考え方」だけを参考にした**完全オリジナル**。
- **重要な方針：任天堂（や他社）の素材・キャラ・名称・デザイン・音は一切使わない。** すべて自作 or CC0 or オリジナルAI生成。
- 主人公「コタ」＝青いキャップに黄色い星マークが目印。収集アイテム「コタムーン」＝黄色い月に顔。帽子「チェイス」＝青い帽子＋黄色い星（オリジナル）。

## ファイル構成
- `main.tscn` … メインシーン（Player / Floor / DirectionalLight3D）
- `player.gd` … コタの操作スクリプト（`CharacterBody3D` にアタッチ）
- `hat.tscn` / `hat.gd` … 帽子「チェイス」（Area3D。投げると飛んで戻る）
- `project.godot` … プロジェクト設定＋入力マップ
- （今後）`kotamoon.tscn` … コタムーン（収集アイテム）

## 操作（入力）
| 技 | 入力 | 実装 |
|---|---|---|
| 移動 | WASD / 矢印 | 入力マップ move_forward/back/left/right |
| ジャンプ（2段・3段） | Space | 入力マップ jump |
| ダッシュ | Shift 押しっぱ | 入力マップ dash |
| ヒップドロップ | Ctrl（空中） | 入力マップ hip_drop |
| ダイブ | 左クリック | コードで直接検出（マップ登録不要） |
| チェイス投げ | E | コードで直接検出（マップ登録不要） |

## コーディング規約・注意
- **インデントはタブ**（スペース混在にしない）。GDScript はインデントで構造が決まるので厳守。
- 主要パラメータは `@export` にしてインスペクターで調整できるようにする（例：`jump_velocity`, `hip_drop_speed`, `dive_speed` など）。
- デバッグ表示は `@export var debug_jump: bool` のトグルで出す。実機確認が終わったら false でもよい。
- 既存の「走る/ダッシュ/2段・3段ジャンプ/追従カメラ」の挙動を壊さない。新しい技はステート（状態）で分け、`hip_active` などのガードで既存処理を止める形にする。
- **`is_on_floor()` の罠**：`was_on_floor` は `move_and_slide()` の**前**に記録し、着地判定は**後**で行う（過去にここでバグが出た。`player.gd` 冒頭コメント参照）。

## 実装済み / これから
- [x] 2段・3段ジャンプ（着地バグ修正済み）
- [x] ヒップドロップ（Ctrl）
- [x] ダイブ（左クリック）
- [x] チェイス投げ（E）
- [ ] コタムーンの配置と収集（5個・左上カウントUI・5個で新エリア）

## 動作確認（大事）
- 変更したら Godot エディタで **F6**（現在シーンを実行）して実機確認する。
- コードのロジックだけの検証なら、フレーム単位の簡易シミュレーション（Python等）で状態遷移を確かめてもよい。
- 1機能ずつ実装し、動作確認でOKが出てから次へ進む。

## Git
- こまめにコミット（1機能＝1コミット目安）。動く状態になったらバックアップも兼ねてコミット/プッシュ。
- コミットメッセージは日本語でOK。何を・なぜ変えたかを1行目に。
